import { Route } from '@angular/router';
import { AlbumloaderComponent } from './albumloader.component';

export const AlbumLoaderRoutes: Route[] = [
    {
      path: 'albumloader',
      component: AlbumloaderComponent
    }
];
